import { Typography } from '@/components/Elements';

const Plan = () => {
  return (
    <div>
      <Typography component="h1" variant="h6" sx={{ml:32}}>
        Add Plan
      </Typography>
    </div>
  );
};

export default Plan;
